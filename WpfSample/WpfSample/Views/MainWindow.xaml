<Window x:Class="WpfSample.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        xmlns:core="clr-namespace:WpfSample.Core;assembly=WpfSample.Core" 
        xmlns:views="clr-namespace:WpfSample.Views"
        Title="{Binding Title}" Height="650" 
        Width="925" MinHeight="300" MinWidth="500">
    <Grid Background="{StaticResource BrushPrimary3}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <views:NavigationView Grid.Column="0" />

        <ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.ContentRegion}" Grid.Column="1" />
    </Grid>
</Window>
