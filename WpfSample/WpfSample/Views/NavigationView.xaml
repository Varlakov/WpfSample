<UserControl x:Class="WpfSample.Views.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">

    <Grid Background="{StaticResource BrushPrimary1}" RenderOptions.EdgeMode="Aliased">
        <Grid MinWidth="300">
            <Grid.Resources>
                <Style BasedOn="{StaticResource NavButton}" TargetType="RadioButton" />
            </Grid.Resources>
            <StackPanel Orientation="Vertical" Margin="10,10,0,10">
                <RadioButton Grid.Column="0"
                        Command="{Binding NavigateToActivitiesCommand}"
                        Content="Activities" />
                <RadioButton Grid.Column="1"
                        Command="{Binding NavigateToEquipmentsCommand}"
                        Content="Equipments" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
